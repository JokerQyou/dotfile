customization:
  distribution_code_name: Weasel
  distribution_version: 0.9.30
  generator: "Rime::SwitcherSettings"
  modified_time: "Fri Sep 18 15:37:33 2015"
  rime_version: 1.1.2
patch:
  schema_list:
    - {schema: luna_pinyin_simp}
    - {schema: luna_pinyin}
    - {schema: cangjie5}
    - {schema: luna_pinyin_fluency}
    - {schema: luna_pinyin_tw}
    - {schema: emoji}
  menu:
    page_size: 7
  switcher:
    hotkeys:
      - "Control+grave"
  ascii_composer/good_old_caps_lock: true  # 使用caps键来切换大小写
  ascii_composer/switch_key:
      Caps_Lock: noop
      Shift_L: commit_code  # 将编码上屏并切换为英文
      Shift_R: inline_ascii  # 临时输入英文（Mac上不可用）
      # Control_L: clear
      # Control_R: commit_text
